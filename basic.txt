diff between var and let
template Strings(literals )
Math.trunc function

for of loop 
for in loop //to get the index of array
for(var i in a)
{
    console.log(i);
}
javascript_info
alter table stock        add constraint FKdm1irxvimc1qe9q09ygaby667        foreign key (company_code)        references company (company_code))</textformat></flashrichtext>
 create table stock (       stock_id integer not null,        current_price double precision not null,        date datetime,        time datetime,        company_code integer,        primary key (stock_id)    ) engine=MyISAM)</